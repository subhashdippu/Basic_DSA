// Radha
// NaN - Not a Number

// const arr = ["a", "b", "c", "d"];

// let obj = arr.reduce((a, it) => ({ ...a, [it]: it }), {});
// console.log(obj);

const arr = ["a", "b", "c", "d"];
console.log(arr);
let obj = arr.reduce((a, it) => ({ ...a, [it]: it }), {});
console.log(obj);
